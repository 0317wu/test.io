# 使用說明

## 簡介

此專案是一個基於 Flask 和 LINE Bot API 開發的健身助手應用程式，提供使用者飲食紀錄、運動指導、體態紀錄、AI 問答及提醒功能。使用者可以通過 LINE 平台與機器人互動。

## 功能一覽

### 1. 主選單
使用者輸入「開始」，機器人將回應主選單，包括以下選項：
- 飲食管理
- 體態紀錄
- 運動目標
- 運動指導
- AI 回答
- 提醒
- 飲食紀錄
- 健身房查詢

### 2. 飲食管理
- **飲食管理**：從BMI提供給您一日飲食計畫

### 3. 體態紀錄
- **輸入紀錄**：記錄體重和身高，並計算 BMI。
- **查看紀錄**：顯示體重和 BMI 的歷史數據。
- **顯示體重圖表**：生成體重和 BMI 趨勢圖。

### 3. 運動目標
- **減脂計畫**：顯示針對減脂的運動計畫。
- **增肌計畫**：顯示針對增肌的運動計畫。
- **提高心肺功能計畫**：顯示針對心肺功能的運動計畫。

### 4. 運動指導
- **訓練計劃**：提供初學者、中級者和高級者的運動訓練指導。

### 5. AI 回答
輸入「AI 」+問題，機器人將使用 Google Generative AI 回答問題，例如：「AI 什麼是增肌？」。

### 6. 提醒功能
- **新增提醒**：設定提醒時間及訊息。
- **提醒通知**：按時向使用者推送設定的提醒訊息。

### 7. 飲食紀錄
- **輸入紀錄**：使用者上傳食物照片後，AI 將自動辨識食物名稱並回傳熱量估算值。
- **查看紀錄**：查看以輸入的的飲食紀錄

### 8. 圖片辨識
- **食物熱量計算**：使用者上傳食物照片後，AI 將自動辨識食物名稱並回傳熱量估算值。
- **物品用途說明**：若照片非食物，AI 將辨識物品並提供其用途或使用方式的描述。

### 9. 健身房查詢
- **查詢健身房位置**:使用者傳送自己位置後，系統回傳附近健身房位置。

## 安裝與設定

1. **克隆此專案**
```bash
$ git clone https://github.com/0317wu/test.io.git
$ cd test.io
```

2. **安裝依賴**
```bash
$ pip install -r requirements.txt
```

3. **設定環境變數**
建立 `.env` 文件，並填寫以下內容：
```env
LINE_CHANNEL_ACCESS_TOKEN=你的Line頻道存取金鑰
LINE_CHANNEL_SECRET=你的Line頻道密鑰
GOOGLE_GENERATIVEAI_API_KEY=你的Google Generative AI API金鑰
SERVER_URL=你的伺服器網址（例如：http://yourdomain.com）
```

4. **初始化資料庫**
- 初始化 `food_record.db`：
  ```bash
  $ python -c "from modules.food_record import initialize_database; initialize_database()"
  ```

5. **啟動應用程式**
```bash
$ python app.py
```

6. **部署伺服器**
使用 Nginx 或其他 Web 伺服器將應用部署到生產環境。

## 使用說明

### 上傳圖片
1. 使用 LINE 上傳圖片。
2. 若照片為食物，機器人將回傳每項食物的名稱及估算熱量。
3. 若照片為其他物品，機器人將回傳物品名稱及使用方式的描述。

### 主選單
- 輸入「開始」查看功能選單。

### 設定提醒
1. 輸入提醒時間與內容。
2. 應用將在指定時間推送提醒訊息。

### 查看紀錄
- 查看飲食、體態和運動的歷史數據。

### 使用 AI 問答
- 在文字前加入「AI 」即可提問。

## 開發者信息

此應用程式由 Flask 框架構建，並集成了以下功能：
- **LINE Bot API**：與使用者互動。
- **Google Generative AI (Gemini)**：提供圖片辨識與文本生成。
- **Matplotlib**：生成圖表。
- **SQLite**：存儲使用者數據。
- **APScheduler**：管理提醒功能。

歡迎回饋或提交 Pull Request！
